[project]
name = "cooking-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nobrien1", email = "nateobrien.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
cooking-core = "cooking_core:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
